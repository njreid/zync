*.iml
.gradle
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Release signing secrets — NEVER commit these
key.properties
*.jks
*.keystore

# Node V8 compile cache — build artifact, not source
/node-compile-cache/

# Task/session tooling, not part of the app
/.superpowers/

# adb capture logs from emulator verification
adb*.log
